#set($attribs = $ctx.args)
#set($attribs.createdBy = $ctx.identity.username)

{
  "version": "2017-02-28",
  "operation": "PutItem",
  "key": {
    "artistId": $util.autoId()
  },
  "attributeValues": $util.dynamodb.toMapValuesJson($attribs)
}